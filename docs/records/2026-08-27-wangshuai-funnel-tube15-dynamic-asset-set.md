# Wangshuai funnel/tube dynamic asset set v2

Scenario Forge imports the promoted ConvertAsset dynamic set byte-for-byte into:

`outputs/scientific_workbench_funnel_tube15_liquid_asset_set_v2_20260827/`

This v2 directory is the default consumer entry. The prior
`scientific_workbench_funnel_tube15_liquid_asset_set_20260826` directory remains
an immutable exact-source kinematic fixture and is not overwritten.

The v2 set contains a dynamic threaded 15 mL tube, dynamic closure, dynamic
small-v2 funnel, and the unchanged 1,948-particle overlay. Scenario Forge does
not author collider, SDF, mass, inertia, material, or warning-suppression
overrides; those remain producer-owned. Each package keeps an identity entry
root and package-local dependency closure.

ConvertAsset's Isaac Sim 4.1 evidence qualifies three rigid cold starts per
instrument and three stationary funnel-to-tube PBD cold starts. Loaded open-tube
transport remains unqualified and is exposed as
`dynamic_loaded_liquid_transport=false`. Robot, task, benchmark, thread
engagement, and measured physical parameters remain outside the claim.
