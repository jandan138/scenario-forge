# 2026-09-12 齿轮内侧支撑前缀诊断编译

复用已有原生场景变体入口，接入 ConvertAsset 的两种齿轮与共同内侧支撑。
没有修改编译器代码、任务指令或原目标判据；物理干预由生产者负责，执行与记录由 EEOS 负责。

r1 编译安装至 `outputs/ebench_gear_support_r1_20260912/`。生产者随后增加支撑仅与
目标交互的碰撞组和支撑侧报告，r2 新建至 `outputs/ebench_gear_support_r2_20260912/`，
任务名称为 `gear_support_r2_control_full`、`gear_support_r2_leadin_full`，在
`eeos_evolution` 命名空间注册。旧目录未覆盖。

源和输出哈希检查、实际编译及注册完成。EEOS 正在运行 r2 的 220 步诊断。
因为目标与原桌面碰撞被过滤，该设置只用于前段支撑实验，不具有完整安装任务资格。
`full` 是既有编译器的原始布局标签，不代表本诊断可按完整任务计分。

适用 ASSET-001/002/003、USD-001/002、VAL-002/003/004。规范无需变更：使用既有
源绑定原生原型接口，保留生产者和运行方的职责与证据范围，没有新增通用包能力。

后续编译安装了 12 mm r3 候选于 `outputs/ebench_gear_support_r3_20260912/`。
运行方报告 r2/r3 斜面设置未稳定初始化，不能标记为已通过运行资格；编译器未因
安装成功修改这些资格状态。EEOS 的新诊断读取还需完成只读性校验，详细记录见其
`docs/records/2026-09-12-ebench-gear-inner-support-probe.md`。

另编译安装了源初始姿态对齐的 r4 候选，输出
`outputs/ebench_gear_support_r4_20260912/`。只是接入生产者新版本，未修改编译器或
原任务语义；运行资格仍由 EEOS 的初始状态和接触检查判定。
