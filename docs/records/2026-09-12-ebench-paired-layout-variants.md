# 2026-09-12 原生 EBench 成对布局变体

为后续新实例评测接入共同布局集合，本次扩展已有 scene-variants 原型编译入口。
每个几何条件应用相同 `layout_variants`，仅覆盖既有对象的位置和方向，拒绝通过该
字段改变资产、缩放、指令、判据或机器人。逐条件记录实际改动及布局 ID。

先添加测试确认旧实现未展开布局，随后实现。六项适配器测试与 Ruff 通过；完整
`make check` 正在运行，日志由 EEOS 保存在 `validation_20260912/scenario_forge_layouts.log`。
新输出记录 build-config 哈希，安装前还核对 source-layout；旧 manifest 保留兼容路径。

实际编译 `outputs/ebench_gear_layout_candidates_v1_20260912/`：20 个布局 × 两个几何
条件，共 40 个候选任务。输入由 EEOS 根据源几何和布局生成，统一平移夹具及目标，
并围绕目标几何中心改变其初始 yaw。目标资产、任务指令与判据保持原值。

这些只是固定资产对的新布局候选，未安装为确认集，也未运行模型或通过运行时资格。
EEOS 已核对各对 task_data 相同，所有源包络仍位于桌面内；这不能代替可达性和动态验证。

适用 ASSET-001/002、USD-001/002、STATE-001/004、VAL-002/003/004。
规范无需变更：仍是源绑定原生原型，新增局部参数而未扩大可移植包或运行能力。
已同步原生干预套件设计文档及本记录。
