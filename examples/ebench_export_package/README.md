# EBench Export Package Example

This directory shows the lightweight adapter artifacts written by:

```bash
scenario-forge export ebench --package ./pkg
```

The files are static descriptors for downstream EBench-compatible tooling. They
do not represent episode execution, model evaluation, traces, or benchmark
reports.
